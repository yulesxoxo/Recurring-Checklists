# TODO

- Replace the biweekly anchor date input with a nicer picker/control.
  - It should clearly communicate that the anchor date is an actual reset date.
  - It should only offer dates matching the selected reset weekday.
  - It should make the previous/next reset preview easy to understand while editing.
